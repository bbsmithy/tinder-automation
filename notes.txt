This will be used as a command line program
The program flow is as follows:

1) Login -> if the config file contains login settings use that otherwise use CLI
2) Fetch all matches and length or matches
3) Find matches keep track of likes
4) Fetch all matches again and compare lengths getting the new ones id
5) Message all new matches with message rand selected from array in config file
6) Prompt for viewing new matches/message sent and success rate stat -> open html file that loads json

